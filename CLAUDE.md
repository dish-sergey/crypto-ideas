# crypto-ideas

Слой данных торговой системы (этап 0 дорожной карты). Спецификация — в Google
Drive «Trading Bot — Спецификация», ключевые документы: 00 (архитектура),
04 §1 (правила данных), 09 (источники данных — этот проект реализует его §5).

## Стек

- Java 25 (Liberica), Gradle 9.5 (Groovy DSL), Spring Boot 4.0.6 (без web)
- SQLite (org.xerial:sqlite-jdbc), Jackson, java.net.http (включая WebSocket)

## Запуск

```
./gradlew bootRun                                   # планировщик + WS-ликвидации
./gradlew bootRun --args='--collect=all'            # разовый сбор всего и выход
./gradlew bootRun --args='--collect=funding,oi'
./gradlew bootRun --args='--collect=kraken'                      # Kraken Futures: часовой funding + tickers
./gradlew bootRun --args='--backfill=ohlcv --symbols=BTCUSDT --interval=1m --from=2023-01-01'
./gradlew bootRun --args='--backfill=funding-okx'
./gradlew bootRun --args='--backfill=onchain --from=2015-07-01'
./gradlew bootRun --args='--backfill=oi-archive --symbols=BTCUSDT,ETHUSDT --from=2021-01-01'  # ретро-OI из bulk-архива
./gradlew bootRun --args='--s5-dry-run=once'                    # S5: дайджест ближайших разлоков в Telegram и выход
./gradlew bootRun --args='--s5-dry-run'                         # S5: интерактивный dry-run (живой фид + марки Kraken + мок-исполнение)
./gradlew bootRun --args='--s5-kraken-check'                    # S5: read-only проверка ключа Kraken (без ордеров)
./gradlew bootRun --args='--s5-demo'                            # S5: показ жизненного цикла сделки в Telegram (без ордеров)
./gradlew bootRun --args='--s5-live'                            # S5: LIVE — РЕАЛЬНЫЕ ордера на Kraken Futures
./gradlew bootRun --args='--report=regime-dash --out=reports/dash'  # весь HTML-дашборд режима: index.html (меню) + 5 графиков
./gradlew bootRun --args='--theory=s5-import'                    # ТЗ 65/66: датасет событий S5 в data/theory.db (разово, сеть)
./gradlew bootRun --args='--theory=s5-premium --out=reports/theory' # док. 131: премия S5 на слитых событиях, разрезы Kraken/годы/порог
./gradlew bootRun --args='--theory=basis-history'                  # ТЗ 67: непрерывная минутная история спот+перп для базиса (сеть)
./gradlew bootRun --args='--theory=basis-stress'                   # ТЗ 67: то же по самым волатильным дням (стресс-выборка)
./gradlew bootRun --args='--theory=alloc --out=reports/theory'   # ТЗ 65: стенд аллокации поверх пула S1–S12
./gradlew bootRun --args='--theory=kelly --out=reports/theory'   # ТЗ 66: размер позиции S5 (Kelly с усадкой)
./gradlew bootRun --args='--theory=ou --out=reports/theory'      # ТЗ 67: калибровка и допуск OU (предтест блока B)
./gradlew bootRun --args='--theory=band --out=reports/theory'    # ТЗ 68: полоса бездействия и граница разорения
./gradlew bootRun --args='--theory=verify --out=reports/theory'  # ТЗ 72: доверификация базиса без прореживания + сверка констант S5
./gradlew bootRun --args='--revx-screen --hours=96 --to=2026-09-01T09:00:00Z --revx.db-path=data/revx-oos.db'  # скрининг всех 23 пар: спред, поток, разрешение хеджа, δ*
./gradlew bootRun --args='--revx-exec-report --journal=data/exec/ea.db --out=reports/revx_exec_a.md'           # живой журнал: разложение + пошлина через markout
./gradlew bootRun --args='--revx-replay --journal=<путь> --model=recorded|market|touch'   # СВЕРКА: прогон записи через тот же котировщик
./gradlew bootRun --args='--revx-forecast --journal=<путь> --offsets=10,7'                # ПРОГНОЗ: N ботов по одной книге, вилка по двум моделям
./gradlew bootRun --args='--revx-ladder --journal=<путь> --offsets=6,7,8,9,10,11,12'      # лестница отступов, каждая ступень ОТДЕЛЬНЫМ ботом, плюс κ
```

**Стенд исполнения (`revx/replay/`, 05.09.2026).** Котировку считает ОДИН код —
боевой `QuoteLoop`. Различаются только три границы с внешним миром: `Venue`
(площадка: `TradeClient` шлёт HTTP / `SimVenue` отвечает по данным), `Clock`
(`Clock.system()` / `SimClock` с прыжками по записанным отметкам) и `FairSource`
(`StandReader` / `ReplayFair`). Обмен идёт строками JSON намеренно: иначе стенд
перестанет гонять сборку запроса и разбор ответа, а самые дорогие ошибки жили
именно там.

Моделей исполнения три, и у каждой своя роль: `recorded` — КОНТРОЛЬ (исполнения
из записи, при других параметрах бессмыслен), `market` — РАБОЧАЯ (очередь по
видимой книге), `touch` — ВЕРХНЯЯ ГРАНИЦА (ТЗ §4.3 предупреждает, что она
неверна; годится как заведомо завышенная оценка). **Прогноз без вилки не
отдавать.**

⚠️ **СВЕРКА КОТИРОВОК ЗАВИСИТ ОТ ВХОДА, КОТОРЫЙ ДО 10.09.2026 НЕ ЗАПИСЫВАЛСЯ.**
Живой бот раздвигает отступ на долю `budgetPressure` (дефицит постановок), а
ведро постановок — общее живое состояние без истории: восстановить задним
числом нельзя. Повтор котировал по НЕРАЗДВИНУТОМУ отступу, и у бота A это дало
**0.62% совпавших котировок при полностью исправном боте** (10.09.2026):
давление 0.00336 сдвигало цену на треть процента отступа, чего хватало, чтобы
она легла на СОСЕДНИЙ тик, а сравнение идёт до знака. Одно и то же значение
воспроизводит и бид, и аск до пятого знака — причина установлена. Исправлено:
живой пишет применённую долю в каждый тик (`exec_quote.pressure`), повтор берёт
её оттуда. **Записи до 10.09.2026 этой колонки не имеют, и сверка котировок на
них недостоверна** — прежние 99.92% относятся к окну, когда давление было нулём.
Второй недостающий вход, найденный тогда же: гейт по опоре (`dyn_offset`)
включался только в боевом пути.

После выкатки (10.09 08:20) сверка даёт **100.00% на всех трёх ботах**, худшее
отклонение 0.00 б.п. ⚠️ Но давление на этом окне было НУЛЁМ — ведро постановок
полное после тихой ночи, — то есть прогон подтверждает целость проводки, а не
саму правку. Правка проверится, когда суточный расход снова перевалит за пол в
100 постановок и давление станет ненулевым.

Сверка ИСПОЛНЕНИЙ этим не задета — она сравнивает сделки, а не цены:
10.09.2026 на 16 ч живой торговли контрольная модель разнесла 72 из 77 живых
исполнений (A 56/60, B 9/10, C 7/7), рабочая угадала 59 из 77 (76.6%) при 6.3%
выдуманных, верхняя граница 63 из 77. ⚠️ Прежнее «бот B — только 72.6%, причина
не разобрана» объясняется иначе: у B котировки расходятся ровно там, где
расходится инвентарь (42.20% против 42%), то есть это снос траектории после
пропущенного исполнения, а не поломка прибора.

⚠️ Параметры берутся ИЗ события `boot` в журнале (машинная часть после `|`), а не
из `application.properties`: живому боту половина приходит из systemd-юнита, и
подстановка умолчаний однажды превратила сверку в сравнение двух разных настроек.
Записи старше 05.09.2026 машинной части не содержат — повтор на них падает
намеренно.

Дашборд режима: `--report=regime-dash` рендерит `index.html` (меню с карточками версий:
текущее состояние, сколько дней в нём, лента за год) плюс `regime-v5.html` (прод), `regime-v3.html`,
`regime-v1.html`, `regime-all.html`, `regime-compare.html`. Отдельные графики по-прежнему доступны
как `--report=regime-v5|regime-v3|regime|regime-all|regime-compare [--out=...]`, меню — `--report=regime-index`.
**Период выбирается на клиенте**: данные вшиты в страницу целиком, кнопки 1М/3М/6М/YTD/1Г/2Г/Всё,
поля дат, ◀/▶ (и стрелки клавиатуры) режут окно без перегенерации; окно пишется в hash
(`regime-v5.html#w=30`, `#w=2024-01-01..2024-06-30`) — ссылкой можно делиться. Общий код окна —
`src/main/resources/regime-window.js`, он инлайнится в каждый отчёт вместо `__WINDOW_JS__`.

S5 dry-run читает токен/chat_id из `telegram/s5_bot.txt` (gitignored) либо env
`S5_TELEGRAM_TOKEN`/`S5_TELEGRAM_CHAT`. Деньги виртуальные (MockExchange), данные и Telegram — реальные.
`--s5-live` дополнительно читает ключи Kraken из `kraken/keys.txt` (gitignored) либо env
`S5_KRAKEN_KEY`/`S5_KRAKEN_SECRET`; исполнение настоящее, но ордер только после ручного подтверждения
(Approval Gate) + стоп −30%. **Live и dry-run с одним ботом одновременно НЕ запускать** (один потребитель
Telegram getUpdates, иначе 409). На micro — сервис `s5-live` (см. deploy/SERVERS.md).

База: `data/crypto.db` (WAL). Схема: `src/main/resources/schema.sql`.

## Принципы (не нарушать)

1. **Два времени у каждой записи** (док. 04 §1): время события + `available_at`
   (когда данные стали бы доступны). Бэктест читает только по `available_at`.
   Правило по источникам: свечи → close_time; funding/OI → их ts; on-chain →
   день +30ч; macro → день +1 сутки; календарь/новости/ликвидации → момент
   получения нами.
2. **Идемпотентность**: все записи — upsert по естественному PK. Повторный
   запуск коллектора не создаёт дублей и ничего не ломает.
3. **Изоляция сбоев** (док. 01 §3): падение одного источника логируется и не
   трогает остальные. Binance fapi может быть недоступен из некоторых сетей —
   это ожидаемо, funding тогда идёт только с OKX.
4. **OI и ликвидации невосполнимы** (док. 09 §4): Binance отдаёт OI только за
   30 дней, история ликвидаций бесплатно недоступна. Планировщик должен работать
   постоянно; каждый простой — дыра в данных навсегда.

## Ограничения источников (проверено на живых API 2026-07-17/18)

- **OKX funding-rate-history отдаёт только ~3 последних месяца** — запрос
  старше отдаёт пусто. `--backfill=funding-okx` глубокую историю не даст;
  глубокий funding — только Binance fapi (там, где он доступен). Funding
  тоже частично невосполним — см. принцип 4. С локальной машины fapi работает:
  вся история с первой ставки (BTC 2019-09-10) уже снята обычным `--collect=funding`.
- **Coin Metrics Community больше не отдаёт `CapRealUSD`** (HTTP 403
  «not available with supplied credentials»). Метрика убрана из конфига;
  realized cap выводится арифметически: `CapRealUSD = CapMrktCurUSD / CapMVRVCur`.
- **ForexFactory: `ff_calendar_nextweek.json` удалён** (404), остался только
  `ff_calendar_thisweek.json`. Календарь собирается по текущей неделе.
- **FRED требует HTTP/1.1 и User-Agent `curl/...`** — Java HttpClient c HTTP/2
  получает RST_STREAM, а с collector/браузерным UA запрос молча висит до
  таймаута (Akamai). Оба обхода зашиты в `ApiClient` (глобальный HTTP/1.1 +
  пер-хостовый UA); не менять UA для fred.stlouisfed.org.
- **DefiLlama `api.llama.fi/emissions` стал платным** (HTTP 402), **но корпус
  разлоков бесплатен через CDN их же фронтенда** (проверено 02.09.2026, см. `s5/`):
  `https://defillama-datasets.llama.fi/emissionsProtocolsList` (372 протокола) и
  `https://defillama-datasets.llama.fi/emissions/{slug}` — без ключа и лимитов,
  с форвардными событиями до 2032. Покрывает 113 из 274 перпов Kraken. Грабли:
  `gecko_id` бывает мусорным (маппить по `metadata.token` = `coingecko:<id>`);
  `timestamp` иногда строка; у `unlockType:"linear"` в `noOfTokens` лежит смена
  скорости `[было, стало]`, а не объём; часть адаптеров пишет линейный вестинг
  ежедневными микро-`cliff` (Celestia — 0.02% циркуляции в день), поэтому событие
  S5 отбирается по размеру, а не по типу. Платные альтернативы для оставшихся 35
  монет: CryptoRank `/v2/currencies/{id}/vesting` (Pro), Tokenomist, DropsTab.
- **Bulk-архив `data.binance.vision` (листинг через S3 API)**: `metrics`
  (с OI) — daily с 2020-09-01; `fundingRate` — monthly с 2020-01;
  `bookDepth` — daily с 2023-01-01; **`liquidationSnapshot` удалён совсем**
  (папки нет) — история ликвидаций бесплатно недоступна, принцип 4 в силе.
- **Binance futures WS — рабочий путь `wss://fstream.binance.com/market/stream` + SUBSCRIBE**
  (combined-формат `{"stream":..,"data":{..}}`). Устаревшие `/ws/<stream>` и `/stream`
  отдают только ack `{"result":null,"id":1}` **без данных** — это была причина нулей
  по Binance-ликвидациям (исправлено 2026-07-23). `!forceOrder@arr` — объединённый
  UM+CM поток (поле `st`: 1=UM, 2=CM). С Frankfurt-micro (ЕС) работает; с локальной
  машины хост fstream раньше молчал (geo) — сбор ликвидаций перенесён на micro.
  Bybit WS `allLiquidation` — второй, полный рыночный источник. Диагностика WS —
  `websocat` (musl-бинарь с github vi/websocat).
- **Revolut X: авторизованный API снимает почти все ограничения публичного.**
  Лимиты рыночных данных с ключом — **100 запросов/с и 1000/мин** против 1/с
  публично, поэтому стенд работает по ним (`revx.auth.*`). Ключ Ed25519: пара
  генерируется локально (`openssl genpkey -algorithm ed25519`), публичный
  регистрируется в веб-приложении Revolut X, оно выдаёт 64-символьный API-ключ;
  подписывается склейка `timestamp + МЕТОД + путь_от_/api + query + тело`,
  заголовки `X-Revx-API-Key` / `X-Revx-Timestamp` / `X-Revx-Signature`. Ключ
  выпущен **только на чтение**, лежит на bot-arm в `~/revx/keys/` (в git не
  попадает). Грабли авторизованных эндпоинтов (проверено 19.08.2026):
  - **другая схема ответа**: книга отдаёт `p`/`q`/`no` вместо `price`/`quantity`/`count`,
    сделки — `tid`/`tdt`/`p`/`q`/`s` вместо `id`/`timestamp`/…; парсеры понимают обе;
  - **символ сделок в ПУТИ**: `/api/1.0/trades/all/{symbol}`. Тот же адрес с
    `?symbol=` отвечает `401 Unauthenticated access` — выглядит как проблема подписи;
  - **id сделки без дефисов** (`d307f978621b…`) против UUID публичного пути —
    без нормализации одна сделка попадает в базу дважды;
  - узкое место с ключом — **не лимит, а задержка ответа** (~200 мс): обход 23 пар
    в один поток занимал 9.5 с при периоде 5 с, лечится пулом воркеров;
  - **торговые лимиты** (для будущего этапа): `POST /orders` — 10/с и **1000/сутки**,
    `PUT /orders/{id}` (replace) — 10/с **без суточного лимита**, `DELETE` — 100/с и
    1000/мин. **Формат и поведение проверены живой заявкой 27.08.2026:**
    - `POST /api/1.0/orders` — тело `{client_order_id (UUID), symbol:"BTC-USDC",
      side:"buy|sell", order_configuration:{limit:{base_size, price,
      execution_instructions:["post_only"]}}}`. **`post_only` СУЩЕСТВУЕТ** — вопрос
      ТЗ §6 закрыт, ценовой предохранитель вместо флага не нужен;
    - `PUT /api/1.0/orders/{venue_order_id}` — требует **новый** `client_order_id`
      каждый раз и возвращает **новый `venue_order_id`**: замена создаёт другую
      заявку, состояние надо перечитывать из ответа, а не помнить;
    - `DELETE /api/1.0/orders/{venue_order_id}` — без тела, ответ **204 без тела**;
    - `GET /api/1.0/orders/active` — открытые заявки, `GET /api/1.0/balances` — остатки.
    - ⚠️ **имя поля с идентификатором РАЗНОЕ:** постановка и замена отдают
      `venue_order_id`, список активных — `id`. Уборка, искавшая только
      `venue_order_id`, не нашла собственную заявку и оставила её в книге.
    - ⚠️ **422 на replace НЕ значит, что замены не было** (найдено на живом
      исполнителе 30.08.2026, док. 111). Приходит
      `Cannot replace an order that is not in the 'NEW' state`, а сама заявка при
      этом уже `status: cancelled, reject_reason: replaced` — то есть наследник
      создан, а его идентификатор не пришёл никуда. Заявка остаётся в книге без
      хозяина, её резерв делает инвентарь неотчуждаемым, и попытки продать дают
      `Insufficient balance of ₿0` (766 отказов подряд = весь суточный лимит
      постановок). **Судьбу заявки нельзя выводить из её собственного статуса —
      только из `GET /orders/active`.**
    - ⚠️ **У той же 422 есть ВТОРАЯ причина: заявка исполнена ЧАСТИЧНО** (найдено
      09.09.2026 при переходе на лот $3). Частичное исполнение выводит заявку из
      состояния `NEW`, и заменить её нельзя уже никогда — **дополнить до полного
      размера тоже невозможно**, такого действия у площадки нет. При этом заявка
      **остаётся в книге** и видна в `GET /orders/active` со статусом
      `partially_filled` и своим `filled_quantity` — по этим полям её и надо
      узнавать, не тратя на неё замены. Наблюдено: восемь отказов подряд за 84 с
      по одной заявке, остаток (73% лота) добрался сам. При лоте $1 частичных
      исполнений не бывает вовсе — все 390 живых сделок за сутки ровно в лот;
      с лотом $3 доля отказов 422 выросла с 0.2–0.6% замен до 0.8–1.1%.
      Выбор «ждать или снять и поставить заново» пока не сделан — см.
      `docs/pairs/ЗАДАЧИ.md` §A5-bis, копится статистика (`exec_event` с
      `kind IN ('partial','partial_done')`). ⚠️ Живых случаев на 09.09 17:05
      UTC — **ноль**: за 1 ч 51 мин после выкатки 17 исполнений и ни одного
      частичного (сверено с сырыми ответами). Живой путь пометки ещё не
      сработал ни разу; проверен стендом и тестом `SimVenuePartialTest`.
      **Стенд повторяет это поведение с вечера 09.09** (`SimVenue.replace`
      отвечает 422), и прогоны крупным лотом, посчитанные до этого, завышены —
      BTC 1 × $3 на 13%, 3 × $1 ни на сколько.
    - ⚠️ **Есть и ТРЕТЬЯ 422 — `Could not replace order with id: X`**, и она
      значит совсем другое: заявка при этом жива и в состоянии `new` (проверено
      списком активных через 40 мс). Это временный отказ площадки, и правильная
      реакция — обычная пауза с повтором, а не разбор судьбы. Различать их можно
      только ПО ТЕКСТУ: код 422 у всех трёх один. Доля за сутки 09.09.2026 —
      17 из 332 (5%): A 8 из 189, B 4 из 71, C 5 из 72.
    - ⚠️ **БОТ ПОКАЗЫВАЕТ СВОЙ ЗАХВАТ, А НЕ ОСТАТОК СЧЁТА** (разбор 10.09.2026 по
      вопросу владельца «на Революте биткоины есть, а бот показывает 0»). Счёт
      ОБЩИЙ на всех ботов, поэтому при `own-position=true` бот считает своим
      только то, что записано за ним в реестре `alloc.db`. Ноль у бота и
      ненулевой остаток на счёте — не противоречие и не поломка: это значит, что
      монета НИЧЕЙНАЯ. Так и было: счёт 0.00003829 BTC (лот + пыль), в реестре
      у `a` 1.4e-20, у `b` ноль, из четырёх открытых заявок ни одной продажи
      BTC — то есть остаток свободен и не заперт резервом. Журнал бота при этом
      сходится сам с собой ДО НУЛЯ: захвачено 2 лота, куплено 10, продано 12.
      Дублей исполнений нет (22 записи, все с разными `venue_id`, все ровно в
      лот) — то есть это не повтор счёта, как было на ADA 08.09, а расхождение
      реестра с реальностью, накопленное при захвате на старте.
      **Лечится ТОЛЬКО через `/claim` или перезапуск бота** (он захватывает
      свободный остаток при инициализации); править `alloc.db` руками нельзя.
      Диагностика — `--revx-trade-check` (только GET, ордера не шлёт): печатает
      `/balances` и `/orders/active`.
    - ⚠️ **Проверять остаток надо по `available`, а не по `total`:** средства под
      стоящей заявкой в `total` видны, а поставить на них нельзя. Своя же заявка
      доступное расширяет — replace резерв возвращает.
    - Минимум заявки связывает `min_order_size_quote` = **0.1 USDC**
      (`min_order_size` = 1e-8 BTC пренебрежим). Остаток от частичного
      исполнения бывает мельче и постановкой не проходит.
    Перевыставление обязано идти через replace: схема «отменить и поставить
    заново» упирается в 1000/сутки уже при наблюдённой активности. **1000/сутки —
    это лимит на ВСЕЛЕННУЮ, а не на темп:** постановки тратятся после исполнений,
    измерено BTC 578 + ETH 132 = 71% суток (док. 74), то есть торговать можно
    две-три пары, а не 20 из ТЗ §3.4.
  - **СЕМАНТИКА ЛИМИТОВ ЗАКРЫТА 07.09.2026** (документация + замеры на шести
    живых ботах). Раньше это была догадка, теперь — установленное:
    - лимиты заданы **ведром токенов**, а не счётчиком с обнулением: у постановки
      «10 tokens/second» И «1,000 tokens/day», у replace только «10 tokens/second»
      **без суточного**, у cancel и всех GET — 100/с и 1000/мин;
    - **полночь ни при чём.** Ведро пополняется непрерывно. Доказано на
      секундном ведре: в 429 приходит `Retry-After` 334 и 507 мс при 19 PUT в
      секунду, то есть ровно «сколько ждать до следующего токена» при 1 токене
      на 100 мс. Суточное ведро — тот же механизм, 1 токен на 86.4 с;
    - **`Retry-After` в МИЛЛИСЕКУНДАХ** — теперь это не наблюдение, а слова
      документации: «The `Retry-After` header specifies the delay in milliseconds»;
    - **лимит общий на ВЕСЬ АККАУНТ, не на бота и не на ключ.** Доказано тем, что
      429 прилетают разным ботам в одну и ту же секунду (09-06 21:30:47 — a, c×2,
      d×2, f×3). Документация подтверждает: «Each API key directly maps to the
      user account». Второй ключ второго лимита НЕ даст, суб-аккаунтов у площадки
      нет (проверено 04.09.2026 — адреса `/accounts`, `/subaccounts` отсутствуют);
    - **суточное ведро мы ни разу не пробивали.** 29.08.2026 бот A сделал 774
      постановки за сутки — все прошли, ни одного отказа по квоте. Прежняя запись
      «766 отказов = весь суточный лимит» была ВЫВОДОМ, а не наблюдением: те 766
      отказов деловые (`Insufficient balance`), площадка их приняла. Расходуют ли
      отказы квоту — по-прежнему неизвестно, и мы считаем их своими (консервативно);
    - ⚠️ **упираемся мы в 10/с на replace, а не в суточное.** За 19 ч после
      правки «одна замена за тик»: 189 428 PUT (2.75/с в среднем), 19 секунд выше
      10/с, 45 отказов. Стоят они по 2–569 мс каждый, то есть пренебрежимы;
    - измеренный СПРОС на постановки, 24 ч шести ботов: BTC 218, ADA 217, ENA 131,
      PEPE 111, ETH 99, SOL 95 = **871 из 1000**. Бюджета хватает; беда в том, что
      он нарезан на неподвижные доли, и 07.09 ADA встала на 11 ч, а PEPE на 7 ч,
      пока у SOL и ETH простаивало 240 неиспользованных постановок.
- **Revolut X (публичный путь) — ограничен МИНИМАЛЬНЫЙ ИНТЕРВАЛ между
  запросами (~1 с), а не средний темп.** Замеры 19.08.2026: одиночные запросы по
  1 req/s — чисто, с 1.25 req/s — устойчивый 429; но два запроса **встык** дают
  429 на втором при любой паузе между парами (проверено на 2.5/4/6 с — всегда
  ровно 50%). Поэтому у клиента стенда бакет с **ёмкостью 1** (никаких залпов),
  рабочий темп 0.8 req/s, а две ноги пары идут последовательно со skew ~1.25 с —
  одновременный опрос с одного IP невозможен. Лимит **не пер-эндпоинтный**:
  размазывать нагрузку по символам бесполезно. В 429 приходит `Retry-After` в
  **миллисекундах** (375/531/919), вопреки спеке; на подтверждённой перегрузке
  прилетало 27000. Каталог пар отдаётся **словарём** с ключом `ETH/USDC`, а в пути
  книги символ пишется через дефис (`ETH-USDC`). Массив `asks` — **в убывающем
  порядке** (324 снимка из 324), `asks[0]` это худший аск: наивное чтение завышает
  спред в полтора раза. Лента сделок отдаёт поле `side`. Публичные endpoint — без
  ключа, `region=EEA` доступен и с локальной машины.
- **Шаг контракта перпа Kraken у каждой пары свой** (`contractValueTradePrecision`
  из `instruments`), и он решает, возможен ли хедж вообще. При потолке инвентаря
  в $20 (живой бот) в него укладывается 2.6 ступени у BTC, 8.1 у ETH, **19.2 у
  SOL** — то есть на живом масштабе хедж не грубоват, а невозможен ни на одной из
  трёх пар. Прогоны ETH/SOL из док. 125 шли с BTC-шагом 0.0001 на все пары, то
  есть с разрешением на два порядка тоньше настоящего. Карта шагов —
  `revx.sim.hedge-steps`, снимается командой `--revx-screen`.
- **Целевой шорт округляется ВНИЗ** (`revx.sim.hedge-round-down=true`).
  Округление «к ближайшему» на грубом шаге переворачивает позицию: измерено на
  живом масштабе BTC — нетто-шорт 5e−5 BTC = 20% потолка в обратную сторону.
- **Kraken Futures** — публичный API без geo-блока и ключа. Funding здесь
  **ЧАСОВОЙ** (не 8ч), берётся через `historicalfundingrates` **API v4** (v3 →
  404), ~12 мес в одном ответе. Ставки Kraken невзаимозаменяемы с OKX/Binance
  (док. 09 §4.7) — храним отдельно в `kraken_funding` / `kraken_ticker`.
  **Минутные марк-цены перпа** — `api/charts/v1/mark/{perp}/1m?from=&to=` (без
  ключа, куски по 12 ч), кэшируются в `revx_perp_mark`: прогон симулятора обязан
  быть воспроизводимым, поэтому в сеть ходит только первый.
- **Зеркало (`--revx-mirror`) НЕСОВМЕСТИМО с настоящим базисом перпа.** Оно
  инвертирует ряд спота, а марки перпа настоящие, поэтому их разность перестаёт
  быть базисом: измерено 1921 б.п. среднего «базиса» на зеркале против 5.2 б.п.
  на реальном окне (док. 146 §6). Предохранитель по дислокации на таком ряде
  срабатывает на каждом окне и выключает хедж целиком. Падающее окно для любых
  вопросов про хедж нужно **реальное**, либо зеркалить перп вместе со спотом.
- **⚠️ У ПЛОЩАДКИ ЕСТЬ ПРЯМАЯ КНИГА `USDC/USD`, А МЫ СЧИТАЕМ КУРС МЕДИАНОЙ.**
  Найдено 10.09.2026 по вопросу владельца «зачем нам курс, посмотрим его прямо».
  Пара есть в каталоге, книга живая и глубокая: бид 1.0000 на 5152 USDC, аск
  1.0001 на 14117, спред **1 б.п.**, пять уровней с десятками тысяч. Мы её ни
  разу не собирали.
  - **наша медиана с ней НЕ СХОДИТСЯ.** Три одновременных замера: прямая книга
    неподвижна на 1.00005, медиана implied дала 0.999744 / 0.999710 / 0.999065,
    то есть расхождение **3.1, 3.4 и 9.9 б.п.** — при боевом отступе 12 б.п.;
  - **медиана ещё и гуляет:** 7 б.п. за 43 секунды, пока прямая книга стоит.
    Это и есть механизм давно замеченного «наша `fair` шумит в 2.4–3.9 раза
    сильнее самой книги»;
  - причина видна в разбросе: implied по 23 парам лежат от 0.998001 (ENA) до
    1.001512 (SUI) — **35 б.п.**, и медиана скачет между соседями. `implied` —
    это отношение середин ДВУХ РАЗНЫХ книг одного актива, и оно наследует
    разность их спредов и потока, а не только отклонение привязки;
  - ⚠️ **КУРС — ПАРАМЕТР ПЕРВОГО ПОРЯДКА** (опыт 10.09.2026,
    `-Drevx.fair.fixed-rate=1.0`): та же лестница на 04–06.09 дважды, подменён
    ТОЛЬКО делитель цены, все гейты прежние. Сдвиг курса на **1.1 б.п.** дал
    BTC @12 б.п. +0.1447 → −0.0241, ETH @12 +0.4383 → +0.0491, SOL @8 +0.6533 →
    +0.4976. ⚠️ **Читать это как «базисный пункт решает прибыльность» — ОШИБКА,
    и она была допущена в первом отчёте.** Опыт не изолировал цену: вместе с ней
    изменилось ЧИСЛО СДЕЛОК (BTC @12: 81 → 49, то есть −40%), значит
    сравнивались разные их наборы. Верная формулировка: положение `fair`
    относительно книги круто управляет ЧАСТОТОЙ исполнений, а доход идёт следом.
    Механизм: `fair` у BTC на том окне лежал на 4.55 б.п. ниже середины его
    книги, бид стоял не в 12 б.п. от рынка, а в 16.5; курс 1.0 отодвинул его до
    17.6; при κ = 0.385 это −35% покупок (наблюдалось −40%), а продажи упали
    следом, потому что спотовый бот не может продать больше купленного;
  - **κ ≈ 0.385 НА БАЗИСНЫЙ ПУНКТ** — главное число про исполнения, замерено по
    лестнице BTC (04–06.09, лот $3): 110.7 сделок в сутки при 8 б.п., 61.7 при
    10, 27.0 при 12, 11.0 при 14. **Один базисный пункт расстояния стоит трети
    потока.** Отсюда: сравнивать настройки по доходу, не глядя на число сделок,
    нельзя;
  - ⚠️ **ГИПОТЕЗА «СМЕЩЕНИЕ ОБЪЯСНЯЕТ НЕДОБОР СДЕЛОК» ПРОВЕРЕНА И ОТВЕРГНУТА.**
    Соблазн был силён: один базисный пункт расстояния стоит трети потока
    (κ = 0.385, замерено по лестнице BTC), значит смещение `fair` от книги
    должно объяснять хронический недобор стенда. Прямое сличение на ОДНОМ окне
    (09.09 15:14 → 10.09 07:40, BTC, лот $3, отступ 12 б.п.): живьём 60 сделок
    (29 покупок, 31 продажа), стенд 28 (15 и 13) — недобор 2.1× при живом
    смещении всего −0.13 б.п. Смещения нет, а недобор есть: причина другая;
  - ⚠️ **ПРЯМАЯ КНИГА ПОЧИНИТ ШУМ, НО НЕ СМЕЩЕНИЕ.** Смещение справедливой цены
    от середины СВОЕЙ же книги равно `implied_пары − rate` и остаётся при любом
    СКАЛЯРНОМ курсе — хоть медиане, хоть прямой книге. Живьём за 16 ч (09.09
    15:14 → 10.09 07:40): BTC −0.13 б.п. (котировки симметричны, бид 11.81
    против аска 11.70), SOL −1.91, ETH −3.77. На срезе 05.09 те же величины были
    −4.56, −5.20, −9.98 при разбросе по 23 парам в 16 б.п. — то есть смещение
    зависит от состава корзины и гуляет день ото дня. ⚠️ Прежняя запись о том,
    что смещение «согласуется с отрицательным markout на BTC и ETH», ОСЛАБЛЕНА:
    у BTC живьём перекоса нет вовсе;
  - ⚠️ ТЗ §4.1 предписывает медиану ИМЕННО чтобы не зависеть от одной книги, и
    это правило разумно. Но оно писалось про выбор «одна из 23 пар против
    медианы», а не «прямой рынок курса против медианы косвенных оценок».
    **Пока НИЧЕГО не менялось:** три замера — не основание переключать
    основание всего котирования. Нужен параллельный сбор `USDC/USD` и сверка
    хотя бы на сутки.
- **КНИГА СОБИРАЕТСЯ НА 20 УРОВНЕЙ ТАМ, ГДЕ КОТИРУЕМ (с 10.09.2026).** Пять
  уровней стоили дороже, чем казалось: счётчики отсева показали, что заявка BTC
  в 12 б.п. невидима для модели в 19% дошедших принтов, а в 14 б.п. — в 38%.
  Заявка уходила за последний собранный уровень, и вся правая часть лестницы
  отступов была занижена систематически.
  - **глубина не стоит запросов** — `limit` параметр того же обращения. После
    выкатки 857 ног за 2 минуты, 429 ноль, отказов ноль;
  - ⚠️ **на публичном пути `limit` игнорируется**: при 5, 10, 20, 50 и 100
    приходит один ответ с пятью уровнями. Глубина есть только с ключом, потолок
    площадки 100 (`limit=200` → 400);
  - охват от середины (замер 10.09.2026): пять уровней — BTC 28.0/14.2 б.п.
    (бид/аск), SOL 39.3/26.5, ETH 36.1/28.4; двадцать — BTC 64.5/194.0, SOL
    106.9/352.8, ETH 110.4/139.7. То есть двадцать перекрывают всю лестницу с
    запасом, а пять на аске BTC не покрывали даже боевые 12 б.п.;
  - хранится текстом в `revx_book.deep_bids` / `deep_asks` (`цена:объём,…`);
    колонки `bp1..bp5` не тронуты, старые базы читаются как прежде, пустая
    колонка глубины — норма. Цена: ~275 МБ/сутки, около двух месяцев диска;
  - ⚠️ **прошлое не чинится.** Глубина есть только с 10.09.2026 вперёд; все
    прогоны по более ранним окнам сохраняют занижение широких ступеней, и
    сравнивать их с будущими напрямую нельзя;
  - ⚠️ `StandAssembler` больше не копирует через `SELECT *`: у частей разное
    число колонок, и звёздочка сложила бы базу до правки с базой после неё.
    Переносится пересечение колонок.
- **⚠️ ПЛОТНОСТЬ СБОРА УПАЛА 07.09.2026 — ОКНА ДО И ПОСЛЕ НЕСРАВНИМЫ.**
  На bot-arm было 23 пары по снимку в СЕКУНДУ (86 тыс. снимков BTC в сутки),
  стало **11 пар по снимку в 1.5 с** (57.6 тыс.). Остальные 12 пар уехали на
  micro2 и собираются там **раз в минуту**. Это сделано намеренно (разложение
  нагрузки, SERVERS.md), но у измерений два последствия:
  - **обходы стенда обязаны считаться на СОБРАННОЙ базе** (`--revx-assemble` из
    копий обоих серверов), иначе опорная вселенная вдвое меньше. Выгрузка
    напрямую с bot-arm даёт 11 пар, и это видно в логе строкой «пар в срезе 11»
    вместо «23» — проверять её перед тем, как верить числам;
  - ⚠️ **даже собранная база не вернёт плотность:** у BTC снимков на треть
    меньше, чем до 07.09, а модель исполнения ловит сделки по снимкам. Прогон
    07–09.09 дал BTC убыток на ВСЕХ отступах против +1.57 на окне 24.08–06.09 —
    это в первую очередь разреженность, а не рынок. Сравнивать доход между
    окнами до и после 07.09 нельзя; сравнивать ступени ВНУТРИ одного окна можно.
  - ⚠️ **СБОРКА ЭТОГО НЕ ЧИНИТ, И НЕ МОЖЕТ.** Проверено 10.09: собрал базу из
    обеих половин (46 символов = 23 пары на все трое суток) и прогнал заново —
    числа совпали с несобранными ДО ЗНАКА, а в логе осталось «пар в срезе 11».
    Причина в гейте перекоса ног: `revx.fair.max-skew-ms=250`, а micro2 ходит
    ПУБЛИЧНЫМ путём без ключа, где минимальный интервал между запросами ~1 с, и
    две ноги пары идут последовательно. Замерено на собранной базе: у BTC
    (bot-arm, ключ) перекос **5 мс**, у DOGE и TON (micro2) — **1250 мс**, то
    есть впятеро выше порога. Все срезы micro2 отбрасываются, и её 12 пар в
    расчёт курса не входят НИКОГДА.
  - Отсюда: **у живых ботов курс USDC/USD считается медианой по 11 парам вместо
    23** (проверено 10.09 — свежие книги есть у ADA, BNB, BTC, ENA, ETH, LINK,
    LTC, PEPE, SOL, XLM, XRP). Медиана по 11 шумит примерно в 1.4 раза сильнее;
    прежний вывод «шум справедливой цены на край не влияет» получен на 23 парах.
    Это не поломка, а цена разложения нагрузки на сервер без ключа, и вернуть
    23 пары можно только ключом на micro2 либо сбором всех 23 на bot-arm.
- **⚠️ МОДЕЛЬ ИСПОЛНЕНИЯ СТЕНДА ЗАНИЖАЕТ СДЕЛКИ В 1.7 РАЗА (сверка 08.09.2026).**
  Первое прямое сличение живых ботов со стендом на одном окне (07.09 19:30 →
  08.09, шесть пар): живьём **390 исполнений**, модель давала **113** — втрое
  меньше. После четырёх правок — **235 из 390 (60%)**: BTC 102/165, ADA 67/145,
  ETH 23/35, SOL 16/12, ENA 15/16, PEPE 12/17. **Числа стенда — по-прежнему
  оценки порядка, а не предсказания, но уже не втрое мимо.**
  - ⚠️ **живые счётчики надо сверять с остатками счёта.** Сумма записей
    `exec_fill` против фактического изменения баланса за сутки: BTC и ETH
    сходятся ДО ЗНАКА, ADA расходилась ровно на один лот — одно исполнение
    записано ДВАЖДЫ (тот же `venue_id`, 10 с спустя: бот повторно прочитал
    накопительный `filled_quantity`). Без этой сверки цель калибровки была бы
    завышена, а инвентарь бота смещён навсегда;
  - ⚠️ **ВРЕМЯ И ЦЕНУ ИСПОЛНЕНИЯ ОТДАЁТ САМА ПЛОЩАДКА — выводить их не надо.**
    В ответе `GET /orders/{id}` есть `updated_date` (момент исполнения),
    `created_date` (момент постановки) и `previous_order_id` (цепочка замен);
    ответ целиком лежит в `exec_request.response`, то есть доступен задним
    числом. Отметка в `exec_fill.ts_ms` — это когда бот УЗНАЛ, а узнаёт он на
    2.4–5.0 с позже (максимум 37 с), и за это время цена уходит ИМЕННО в
    сторону, занижающую отступ. На этой подмене замер «реализованный отступ»
    показал 60 сделок ADA по ту сторону справедливой цены; по `updated_date`
    их 14 из 311, а медианные отступы сходятся с настройкой (BTC 9.3 при 10,
    SOL 18.5 при 18, ADA 11.2 при 14);
  - ⚠️ **`average_fill_price` — НЕ ЦЕНА, а частное `filled_amount /
    filled_quantity` после округления.** Цена заявки лежит в `price`, и на
    ленте принт совпадает с ней ТОЧНО. Запись 08.09.2026 о том, что принт
    лежит на доли базисного пункта за нашей ценой, была ошибкой сравнения: наш
    SELL стоял на 79357.60 — как и принт, — а 79357.50 это округлённое частное.
    Отсюда же и «19 сделок из 164 при точном равенстве»: сопоставлялась не та
    цена;
  - лента и глубина ни при чём: 89% сделок BTC и 80% ADA находятся в собранных
    принтах, снимки книги идут раз в 1.5 с (медиана), почти все исполнения —
    внутри пяти видимых уровней;
  - **потеря 40% объёма была в УЧЁТЕ, а не в модели.** Заявка стенда
    0.00001269, а принт, который её берёт, — ровно 0.00001255, наш же живой лот,
    вернувшийся на ленту. Оставалась пыль в один цент, заявка не закрывалась,
    бот её ЗАМЕНЯЛ — и вместе с заменой исчезала запись об исполнении (узнать о
    сделке он может только по исчезнувшей заявке). Живьём частичных исполнений
    нет вовсе — все 390 сделок ровно в один лот. Исправлено: остаток мельче
    `min_order_size` (0.1 USDC) закрывает заявку;
  - **очередь никто не выбирал.** Модель уменьшала её только сделками, которые
    ДО НАС ДОШЛИ, — но очередь перед нашим бидом состоит из заявок по ЛУЧШЕЙ
    цене, и съедают её ровно те сделки, что до нас не дотягиваются. В отказах
    очередь была больше сделки в 306 раз на BTC, 736 на ADA, 212 на ETH;
  - **принт сам сообщает, что очереди больше нет.** Раз сделка прошла по нашей
    цене или за ней, агрессор до нашего уровня дошёл, а всё, что стояло выше,
    уже выбрано или снято. Значит впереди осталось не больше того, что стоит на
    НАШЕМ уровне; накопленную сумму по верхним уровням держать нельзя;
  - **видимость тоже пересматривается на каждом тике** — по той же причине, по
    какой пересматривается очередь: заявка становится видимой оттого, что ЦЕНА
    ПРИХОДИТ К НЕЙ;
  - **вычитание собственных заявок из очереди ПРОВЕРЕНО И ОТВЕРГНУТО.** Замер
    говорил за него: на 337 живых исполнениях медианная доля нашего объёма на
    уровне нашей цены — 100% на всех шести парах. Но в стенде эффект нулевой
    (BTC 90→90, ADA 58→58): заявка стенда стоит на своей округлённой цене, а не
    на цене живого двойника. Правило, ослабляющее осторожность модели без
    выигрыша, не оставляем;
  - **убыль очереди от чужих отмен** (BTC 0.119, ADA 0.172, ETH 0.271 доли в
    секунду; ⚠️ смещена вверх — мерилась по исполнившимся заявкам) дала нулевой
    отклик. Параметр `-Drevx.queue.decay`, по умолчанию выключен;
  - **что осталось — досягаемость цены, а не очередь.** После правок очередь
    почти не блокирует (BTC и ETH — ни разу), пропуски по невидимости единичны.
    До заявок стенда просто реже доходят принты, и часть этого — предел данных;
  - ⚠️ **знак дохода меняется вместе с числом сделок.** ADA была +0.093, стала
    −0.006: прежний плюс держался на недоборе исполнений. Любой вывод стенда о
    прибыльности, сделанный до 08.09.2026, надо перемерить;
  - **ПРОВЕРКА ВНЕ ВЫБОРКИ (09.09.2026): 415 из 494, то есть 84%.** Вторые сутки
    (08.09 08:50 → 09.09 08:50, с калибровочным окном перекрываются на 5.7 ч из
    24): BTC 119/152, SOL 35/32, ETH 35/50, ADA 182/218, ENA 19/11, PEPE 25/31.
    Вне выборки ЛУЧШЕ, чем на калибровочном окне (60%), и это ожидаемо: правки
    были структурные, а не подгонка параметров — подгонять было нечего. Но
    разброс между сутками (46% против 83% у ADA) больше разброса между
    вариантами настройки, поэтому одно окно решением не считать.
  - ⚠️ **УПАВШИЙ КОТИРОВЩИК ВЫГЛЯДЕЛ КАК ЧЕСТНЫЙ НОЛЬ.** Исключение оставалось в
    потоке прогона, поток тихо умирал, а отчёт печатал «0 покупок, 0 продаж,
    доход +0.0000» — ошибку, неотличимую от результата «настройка не торгует».
    Поймано на PEPE 09.09.2026. Причина падения: у ПЕРВОГО тика справедливой
    цены может не быть (гейты ещё не пропустили), и NaN расползался в стартовую
    кассу и в денежные пределы («заявка превышает предел NaN — не ставлю»).
    Исправлено: опорная цена берётся из первого тика С ЦЕНОЙ, а падение
    котировщика прекращает прогон с сообщением.
  - **ПОВЕРКА ПО КАЖДОЙ ЗАЯВКЕ (`--revx-fill-check`, 09.09.2026): полнота 90%,
    точность 94%.** Прибор берёт все заявки, которые живой бот действительно
    поставил (цены, размеры, время жизни — из журнала запросов), и спрашивает по
    каждой: сказала бы модель, что она исполнится? Обратной связи нет,
    траектория не расходится, поэтому ошибка ПРАВИЛА отделена от ошибки прогона.
    Окно 08.09 08:50 → 09.09 06:50, 296 тыс. заявок:

    | пара | живьём | модель | полнота | точность |
    |---|---:|---:|---:|---:|
    | ADA | 218 | 191 | 86.2% | **98.4%** |
    | BTC | 152 | 141 | 88.8% | 95.7% |
    | PEPE | 31 | 34 | 96.8% | 88.2% |
    | ETH | 50 | 56 | 98.0% | 87.5% |
    | SOL | 32 | 37 | **100%** | 86.5% |
    | ENA | 11 | 13 | **100%** | 84.6% |

    Запаздывание — медиана 0.0 с: когда модель угадывает, она угадывает и момент.
    **Значит «модель занижает на 40%» относилось к СНОСУ ТРАЕКТОРИИ, а не к
    правилу:** в полном прогоне стенд ставит свои заявки, они исполняются иначе,
    расходится инвентарь, за ним скос и цены;
  - ⚠️ **СТОРОНУ ЗАЯВКИ НЕЛЬЗЯ ВЫВОДИТЬ ИЗ ТЕЛА `PUT` — ЕЁ ТАМ НЕТ.** Она
    наследуется по цепочке замен, а цепочка, чей исходный `POST` остался за
    границей выборки, теряет начало. Первая версия поверки в этом случае молча
    подставляла «покупку», и целые слоты ПРОДАЖ приходили в модель покупками
    выше аска — каждый принт ниже такой мнимой покупки давал ложный перехват.
    Так получились числа «точность 35% на SOL, 41% на ETH», продержавшиеся час.
    Правильный источник — справочник `id → сторона`, собранный из ответов
    площадки (`/orders/active` и `GET /orders/{id}`, обе формы содержат `side`);
    после починки точность выросла с 35-41% до 86-98%, а «сторона не
    установлена» осталась у 11 замен из 48 тысяч;
  - ⚠️ **ОЧЕРЕДЬ КАК МЕХАНИЗМ ИСПОЛНЕНИЯ МЕРТВА — работает только перехват**
    (разбор 10.09.2026). «Очередью» модель считает ровно случай
    `t.price() == r.price()`, а зонд показал: почти-равных цен (в пределах
    1e-9) за всё окно НОЛЬ. Дело не в побитовом сравнении — принтов по нашей
    цене не случается вовсе, потому что заявка стенда стоит на своей
    округлённой цене, а лента по этому тику не торгует. Тормоз очереди
    (`q.ahead`) на живом окне 09–10.09 не сработал ни разу; единственный
    действующий ограничитель — видимость (6 пропусков на 28 исполнений);
  - **ЛЕНТА ТОНКАЯ НА НАШЕМ РАССТОЯНИИ, и это объясняет часть недобора.**
    BTC за 16.4 ч: 507 принтов, медиана расстояния от середины книги
    **7.97 б.п.**, дальше 12 б.п. — всего **50** (9.9%), дальше 16 — 23.
    Стенд взял 28, то есть больше половины доступного: модель не жадничает,
    ей нечего брать. ⚠️ Живой при этом взял 60 — больше, чем есть принтов
    дальше 12 б.п. от МГНОВЕННОЙ середины; от середины пятисекундной давности
    их 85. ⚠️ **Гипотеза «живой исполняется потому, что его котировка отстаёт»
    ПРОВЕРЕНА И ОТВЕРГНУТА.** Восстановлена цена РЕАЛЬНО СТОЯЩЕЙ заявки из тел
    запросов (15 853 постановки и замены), время исполнений взято у площадки
    (`updated_date`), посчитан остаток «факт минус предписанное отступом и
    скосом»: медиана 0.00 б.п. и в моменты исполнений, и вообще; хвост 10% —
    −0.5…−1.1 б.п., то есть порог перевыставления 0.5 б.п. и есть. Заявка стоит
    где положено. ⚠️ Вместе с этим отпала и сама посылка: заявка НЕ стоит в
    12 б.п. — скос двигает её на ±10 б.п., и в моменты продаж аск стоял в 9.96
    вместо 12.20. Считать надо полосу 6–12 б.п. (279 принтов), а не «дальше 12»
    (50). Загадка «живой взял больше, чем есть в ленте» была следствием неверной
    посылки;
  - ⚠️ **ОБРАТНАЯ СВЯЗЬ ПО ИНВЕНТАРЮ ТОЖЕ ПРОВЕРЕНА И ОТВЕРГНУТА:** разброс
    инвентаря у стенда и живого совпадает до процента (СКО 0.00004070 против
    0.00004027, медианы одинаковы). Скос работает одинаково, петли нет;
  - 🔑 **НЕДОБОР СИДИТ В СПРАВЕДЛИВОЙ ЦЕНЕ, А НЕ В МОДЕЛИ ИСПОЛНЕНИЯ.** Одно
    окно, одна модель, одни настройки, различается ТОЛЬКО источник `fair`:
    живой бот 60 исполнений, `--revx-replay --model=market` (fair из журнала)
    **50, то есть 83%**, `--revx-pair-sweep` (fair от `StandFair`) **28, то есть
    47%**. Значит модель очереди и перехвата гораздо лучше, чем считалось, а
    теряет вдвое разница между инструментами. ⚠️ **Первое объяснение —
    «`StandFair` считает справедливую цену иначе, чем `StandReader`» — ПРОВЕРЕНО
    И ОТВЕРГНУТО** (10.09, прямое сличение рядов на одном окне, 39 109 тиков):
    медиана расхождения **+0.000 б.п.**, среднее +0.001, 10–90% в пределах ±0.3.
    Две реализации одной формулы дают одно и то же. Различаются инструменты
    вокруг них:
    - **тики**: повтор берёт их из журнала (1 с → 59 201), обход — по снимкам
      книги (1.5 с → 39 419). Расстановщик делает ОДНУ замену за тик, значит
      обход переставляет заявку на треть реже. Главный подозреваемый;
    - **стартовый инвентарь**: у повтора фактический, у обхода ВСЕГДА НОЛЬ
      (`StandFair.toTicks` пишет `inventory = 0`). Пустой счёт при цели 0.3
      даёт скос −0.43, то есть бид в 7.7 б.п. и аск в 16.3 вместо 12/12;
    - вселенная: `revx_pair` со `status='active'` против `revx_book`;
    - поле свежести: `t_sent_ms` против `t_recv_ms` (~200 мс);
    - лот и потолок: из журнала против пересчёта из долларов (±3%).

    На свежих данных с посекундным BTC разрыв сузился с 2.1× до 1.5× (обход 12
    исполнений против 18 живых за 5 ч) — довод за версию про тики, но выборка
    крошечная. ⚠️ Практическое следствие не меняется: `--revx-replay` достовернее
    `--revx-pair-sweep` везде, где есть живой журнал;
  - **перехват ОПРАВДАН.** С `-Drevx.fill.intercept=false` точность 100% на всех
    парах, но полнота падает до 19-41%. Собственная точность перехвата: ADA 98%,
    BTC 94%, ETH 83%, SOL 79%. Он даёт от четверти (BTC) до трёх четвертей (ADA)
    всех исполнений модели и ошибается редко. ⚠️ Прежняя запись «перехват
    ошибается на SOL и ETH в двух случаях из трёх» была следствием ошибки
    стороны и снята;
  - ⚠️ **подтверждение свипа книгой** (следующий снимок обязан показать лучший
    бид не выше цены принта) проверялось на ИСПОРЧЕННЫХ данных и дало «хуже на
    всех парах». Вывод ненадёжен, но и вопрос потерял остроту: точность и так
    94%. Код правила убран;
  - диагностика в отчёте прогона: «объём: исполнено N по M заявкам; бот не
    увидел…», «модель исполнения: очередью N, перехватом M, пропущено из-за
    невидимости K», «очередь заблокировала N раз, медиана очередь/сделка».
  - 🔑 **СЧЁТЧИКИ ОТСЕВА ПО ГЕЙТАМ (10.09.2026) — недостающие принты нашлись.**
    У каждого принта ровно ОДНА метка, сумма граф равна числу принтов, так что
    поток больше не теряется незаметно. Окно 09.09 15:14 → 10.09 07:40, BTC,
    лот , отступ 12 б.п., настройки и стартовый инвентарь из журнала: принтов
    422, нет заявки 14, не дошло 351 (медианный недолёт 5.27 б.п.), ДОШЛО 57 =
    невидима 6 + очередь 0 + **слот уже выбран 21** + объём кончился 0 + взято
    30. Потолок 57 сошёлся с независимым замером по ленте (56). Главная графа —
    мёртвый слот: принт пришёл на нашу цену, пока заявка уже исполнена, а новая
    ещё не поставлена. 30 + 21 = 51 против 60 живых, то есть одна эта графа
    закрывает почти весь остаток разрыва. ⚠️ **ЭТО ОБЪЯСНЕНИЕ ПРОВЕРЕНО И
    ОТВЕРГНУТО В ТОТ ЖЕ ДЕНЬ.** К счётчику добавлен РАЗРЫВ (сколько слот был
    мёртв к приходу принта), и он оказался **0 мс, 20 из 21 в пределах 50 мс**:
    это не задержка восстановления, а одна рыночная заявка, разметающая книгу и
    приходящая на ленту несколькими принтами с ОДНОЙ отметкой времени (четыре
    принта в миллисекунду 1788967328247). Наш единственный лот берёт первый,
    живой бот взял бы ровно столько же. Восстанавливать нечего.
  - 🔑 **ПОСЛЕ СХЛОПЫВАНИЯ ПАЧЕК МОДЕЛЬ ОКАЗАЛАСЬ НА ПОТОЛКЕ.** Считать надо
    отдельные СОБЫТИЯ, а не принты: 12 б.п. от середины книги — 39 принтов, но
    16 событий; 12 б.п. от справедливой цены — 56 и **34**; фактическая цена
    живого бота — 72 и **50**. Стенд берёт 30 из 34, то есть **88%**. Ни
    очередь (0 блокировок), ни видимость (6), ни восстановление слота чинить
    нечего.
  - ⚠️ **ЭФФЕКТИВНЫЙ ОТСТУП ТОЖЕ ПРОВЕРЕН И ОТВЕРГНУТ (девятая гипотеза).**
    `QuoteLoop` копит два ряда на тик: ЦЕЛЬ и цена, РЕАЛЬНО СТОЯЩАЯ в книге
    (`effectiveOffset()`, печатается прогоном). Бид: живой 11.80 медиана
    (10%% 7.72, 90%% 13.84), обход 11.80 (7.72, 13.84), в книге у обхода 11.93
    (8.29, 14.18). Аск: живой 12.20, обход 12.20, в книге 11.84. Совпадает до
    второго знака; у живого цена реально стоящей заявки мерилась отдельно и
    давала остаток 0.00 б.п. к предписанному. Все четыре ряда — один ряд.
  - 🔑 **РАСХОЖДЕНИЕ ОБХОДА С ЖИВЫМ — СВОЙСТВО ТРАЕКТОРИИ, А НЕ ДЕФЕКТ.**
    Сложив замеры 10.09: лента полна (100%% живых исполнений в принтах),
    котировка совпадает, инвентарь совпадает (СКО 0.00004070 против 0.00004027),
    модель берёт 30 из 34 отдельных возможностей — **88%%**, ровно та полнота
    (88.8%% на BTC), что дала поверка по каждой заявке 09.09 на другом окне. Два
    независимых прибора сошлись: ПРАВИЛО ВЕРНО. Совпадают все МАРГИНАЛЬНЫЕ
    распределения, не совпадает совместное с рынком: первое исполнение случилось
    в другую секунду, за ним разошёлся инвентарь, за ним скос, за ним всё
    остальное. ⚠️ Отсюда практическое: **обход не предсказывает число сделок
    живого бота на конкретном окне и не может** — он годится для сравнения
    НАСТРОЕК между собой на одном окне. Сверять живого с моделью надо
    `--revx-fill-check` и `--revx-replay`, а не обходом.
- **Хедж на живом масштабе не срабатывает вовсе.** При лоте $1 и потолке
  0.00025569 BTC инвентарь мельче шага контракта (0.0001), округление вниз даёт
  тождественный ноль — 0 сделок на перпе на всех порогах. Всё, что измерено про
  хедж и базис, относится к СТЕНДОВОМУ масштабу.

## Пары ломаются ПО-РАЗНОМУ — `docs/pairs/`

Одна настройка на шесть пар не работает, и это не вопрос подбора отступа.
Замер 09.09.2026 разложил шесть боевых пар на три класса, и требования к
котировщику у них разные, местами противоположные:

- **BTC, SOL, ETH — рабочие.** Запас (полуспред / ход середины книги за минуту)
  14–26×, поток и исполнения симметричны, край положителен и на минуте, и на
  пяти. ⚠️ Общая беда: наша межплощадочная `fair` шумит **в 2.4–3.9 раза
  сильнее самой книги** (BTC 1.42 против 0.45 б.п./мин) — мы переставляем заявки
  вслед за движением, которого в книге нет, и тратим на это общий лимит
  постановок. **Но на КРАЙ это не влияет:** посчитанный от середины книги, он
  тот же (BTC +4.25 против +4.37 от `fair`, ETH +5.17 против +5.51) — гипотеза
  о «мнимом отборе из-за шума оценки» проверена и отвергнута 09.09.2026.
  Отрицательный markout на BTC и ETH настоящий, а его источник (устаревание
  заявки против осведомлённого контрагента) не разобран;
- **ADA — поток есть, спреда нет.** Запас **1.9×**: спред 21 б.п. при ходе
  5.7 б.п./мин. Захват нормальный (11.2 б.п.), но markout −11.0 съедает его
  целиком; край +0.25 на минуте и **−0.68** на пяти — единственная
  отрицательная пара. Отступом не лечится: весь спред 21 б.п.;
- **ENA, PEPE — спред есть, потока нет.** 30 и 48 сделок на ленте в сутки,
  медианная глубина лучшего аска РАВНА нашему лоту (в книге мы одни). Край
  лучший из шести (+27…+35 б.п.), но поток односторонний, инвентарь набирается
  и не уходит: PEPE стоял у потолка девять часов подряд.

Самая короткая мерка — **оборот / носимый инвентарь**: BTC 55.9, ADA 50.6,
ETH 19.7, SOL 12.8, PEPE 4.0, **ENA 1.8**. Она отделяет маркет-мейкинг от
случайной ставки на цену (но не отвечает на вопрос о прибыльности: у ADA она
50.6 при нулевом крае).

**Решение 09.09.2026: в работе остаются BTC, SOL и ETH.** ADA, ENA и PEPE
выглядят специфически и разбираются отдельно. Их привлекательность в архиве
была ралли: при снятии первых дней (19–23.08, рост 5–7% в сутки) ADA теряет
80% дохода, PEPE 84%, ENA 57%, а оптимальные отступы у них уезжают на смене
окна (ADA 22→14, ENA 38→44, PEPE 32→28) — это шум, а не оптимум.

**Форма сетки: общего правила НЕТ** (13 суток 24.08–06.09, капитал одинаков —
$3 в книге, потолок $20). По СУТОЧНЫМ победам, а не по сумме за окно (сумму
решают отдельные дни):

⚠️ **ПЕРЕСЧИТАНО на исправленной модели** (стенд больше не позволяет двигать
частично исполненную заявку, как и площадка). Числа ниже — новые:

| пара | побед 1 ур × $3 | 2 ур × $1.5 | 3 ур × $1 | доход 1/2/3 | вывод |
|---|---:|---:|---:|---|---|
| BTC | **7** | 2 | 4 | +1.57 / +1.39 / +1.43 | один уровень, 12 б.п. |
| SOL | 3 | **5** | **5** | +3.57 / +4.03 / +3.84 | НЕ один уровень; 2 или 3, 18/16 б.п. |
| ETH | **5** | 3 | **5** | +2.49 / +2.39 / +2.31 | сигнала нет |

⚠️ **Прежнее «у BTC три уровня не выиграли НИ ОДНОГО дня из тринадцати» было
АРТЕФАКТОМ** старой модели. На тех же отступах, где менялась ТОЛЬКО модель,
стало 5/4/4 вместо 8/5/0: ноль держался на способности, которой у бота нет, —
переставить надкусанный трёхдолларовый лот. Вывод «BTC любит один уровень»
выжил, но перестал быть разгромным.

Правка бьёт по КРУПНОМУ лоту: при одном и том же отступе BTC 1 × $3 потерял
13% (+1.81 → +1.57), а BTC 3 × $1 не потерял ничего (+1.42 → +1.43); SOL
1 × $3 потерял 17%. У ETH сдвиг +2…5% на всех формах сразу — это не размер
лота, а то, что жетон замены перестал уходить в обречённый слот.

⚠️ Проверка вне выборки (одни сутки 08–09.09) считалась на СТАРОЙ модели и не
пересчитывалась.

⚠️ Переход на лот $3 меняет риск на сделку: боевые денежные пределы заданы под
лот $1 (заявка ≤ $10, экспозиция ≤ $40, остановка по убытку $1) и в стенде
масштабируются {@code scaleLimitsForLot}, а живьём НЕТ.

Подробности, что именно должен уметь котировщик на каждой паре и как
перемерить — в `docs/pairs/`.

**Как устроен сам бот — `docs/pairs/БОТ-УСТРОЙСТВО.md`.** Алгоритм расстановки
заявок по шагам (справедливая цена и гейты → отступ и скос → два раздвижения →
уровни → зажим по книге и тик → размер → одна замена за тик), повадки площадки,
каталог того, что мы проверяли и что ломалось, и открытые вопросы. Написано на
уровне «можно собрать заново» — читать первым, если возвращаетесь к боту после
перерыва.

## Структура

- `core/Db` — единственный writer-connection SQLite, upsert/batch/queryLong
- `core/ApiClient` — GET с пер-хостовым rate limit и ретраями (429/5xx/IO)
- `collectors/*` — 9 REST-коллекторов, интерфейс `Collector` (name + collect):
  ohlcv, funding, oi, onchain, universe, macro, calendar, news, kraken
- `collectors/OiArchiveImporter` — не Collector, а one-shot импортёр ретро-OI
  из bulk-архива Binance Vision (`--backfill=oi-archive`)
- `ws/LiquidationWsCollector` — ликвидации с 3 бирж: Binance !forceOrder@arr
  (весь рынок), OKX liquidation-orders (все SWAP), Bybit allLiquidation (все ~758
  linear-перпов — список тянется с REST на каждом коннекте, батчи по 100, форс-
  реконнект раз в 6ч для новых листингов); пер-биржевой ping, daemon-потоки
- `scheduling/CollectorScheduler` — расписание (док. 09 §5)
- `cli/CliRunner` — one-shot команды; `cli/CliMode` — переключатель режимов
- `revx/sim/ScreenReport` — `--revx-screen`: вся вселенная разом (спред, поток,
  разрешение хеджа по instruments Kraken, фондирование, `δ*` по ленте)
- `revx/sim/PerpMarkSource` — минутные марки перпа Kraken с кэшем в
  `revx_perp_mark`; без него хедж переоценивался по споту, то есть базис молча
  считался нулевым (док. 146). Три лестницы симулятора, добавленные по док. 142:
  `lot-notional-ladder` (ёмкость), `dislocation-ladder` (предохранитель),
  `hedge-band-ladder` (полоса бездействия, по умолчанию 0 — вредна, док. 144)
- `revx/replay/*` — стенд исполнения: `Venue`/`Clock`/`FairSource` (границы),
  `SimVenue` (учёт заявок и остатков), `FillModel` + три реализации
  (`RecordedFillModel`, `MarketFillModel`, `TouchFillModel`), `MarketData`
  (лента и книга из базы стенда), `BootParams` (настройки из журнала),
  `ReplayRunner` (сверка), `Forecast` (N ботов по одной книге)
- `revx/exec/ExecReport` — `--revx-exec-report`: разложение живого журнала и
  пошлина `c = отступ − захват − markout`

## Что дальше (по док. 09)

- Ретро-OI: импортёр `metrics` из bulk-архива **готов** (`--backfill=oi-archive`);
  осталось прогнать полный backfill 2021→ по BTC/ETH и топ-парам (проверен на 7 днях).
  liquidationSnapshot из архива удалён — для ликвидаций ретроспективы нет.
- Kraken-коллектор **готов** (часовой funding + tickers); осталось при желании —
  `instruments` (спецификации/лотность) и спот-OHLC (резерв), низкий приоритет.
- Разлоки для S5: DefiLlama emissions стал платным — найти замену
  (CryptoRank / Tokenomist) или отложить.
- Ретро-капитализации для survivorship-free вселенной (док. 09 §4.3) — решить
  до этапа 4 (S2).
- MVRV Z-score считается на чтении из onchain_daily, не хранится. CapRealUSD
  в базе нет (см. «Ограничения источников»), realized cap выводится как
  CapMrktCurUSD / CapMVRVCur.
