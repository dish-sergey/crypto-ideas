# Доверификация BASIS, стресс-эпизоды, сверка констант S5 (ТЗ 72)

Прогон `996d5d30-d5b1-4ce8-9acb-737aeda382af`, код `1b2ed044f6ab-dirty`, версия `verify-1.0`.

## Модуль 1: BASIS без прореживания (Q4)

**Фактический шаг прогона 69.** Прореживание было адаптивным: `шаг = ceil(N / 20000)`, при N = 554970 это **28 минут** (19820 точек из 554970). Существенно: прореживались только тесты T1 (ADF, KPSS) и тест разрыва T4; **T3 считался на полном минутном ряде** — это видно в коде `OuAdmission`, где T1 работает с прореженным массивом, а T3 берёт исходный.

Полупериод ряда — **1.7 минуты**, медианная длительность эпизода — **6 минут**. Шаг 28 минут грубее и того, и другого, поэтому подозрение ТЗ обосновано и проверяется прямо.

| Вариант | Определение | Точек | Шаг, мин | ADF | KPSS | T1 | CV(κ) | T3 | Полупериод, мин |
|---|---|---|---|---|---|---|---|---|---|
| FULL | LEVEL | 554970 | 1 | -41.81 (отвергает) | 26.594 (ОТВЕРГАЕТ) | ✗ | 0.74 | ✗ | 1.73 |
| THIN_AS_RUN | LEVEL | 19820 | 28 | -18.08 (отвергает) | 6.889 (ОТВЕРГАЕТ) | ✗ | 2.32 | ✗ | 26.14 |
| THIN_5MIN | LEVEL | 110994 | 5 | -29.06 (отвергает) | 11.645 (ОТВЕРГАЕТ) | ✗ | 0.41 | ✓ | 8.29 |
| FULL | DEVIATION W=480м | 554970 | 1 | -58.83 (отвергает) | 0.005 (не отвергает) | ✓ | 0.95 | ✗ | 1.14 |
| THIN_AS_RUN | DEVIATION W=480м | 19820 | 28 | -39.41 (отвергает) | 0.039 (не отвергает) | ✓ | 2.11 | ✗ | 14.97 |
| THIN_5MIN | DEVIATION W=480м | 110994 | 5 | -46.04 (отвергает) | 0.014 (не отвергает) | ✓ | 0.39 | ✓ | 5.19 |

ADF: отвержение единичного корня — свидетельство стационарности. KPSS: отвержение — против стационарности. T1 требует обоих сразу.

### Чувствительность к окну скользящей средней (§3.2)

| Окно W | Точек | ADF | KPSS | T1 | CV(κ) | T3 |
|---|---|---|---|---|---|---|
| W=60м | 554970 | -115.01 | 0.004 | ✓ | 0.86 | ✗ |
| W=480м | 554970 | -58.83 | 0.005 | ✓ | 0.95 | ✗ |
| W=1440м | 554970 | -52.17 | 0.012 | ✓ | 0.75 | ✗ |
| W=4320м | 554970 | -50.04 | 0.079 | ✓ | 0.74 | ✗ |

Окно задано из логики до прогона: существенно больше полупериода (1.7 мин) и существенно меньше типичной длительности режима funding; стартовое — 8 часов, один интервал расчёта funding.

### T5 отдельной строкой

Независимых эпизодов ≥0.4% на непрерывном ряде — **3** при пороге T5 = 30.

### Вердикт модуля 1

Вердикты `FULL` и `THIN_AS_RUN` по T1 совпадают (✗ против ✗), по T3 совпадают (CV 0.74 против 2.32). Контроль масштаба `THIN_5MIN`: T1 ✗, CV(κ) 0.41.

Прореживание на вердикт не повлияло — вывод прогона 69 по `BASIS` подтверждён на полном ряде.

Отклонения (`DEVIATION`) ведут себя **иначе, чем уровень**: T1 они проходят на 4 окнах из 4 (KPSS не отвергает стационарность, ADF отвергает единичный корень), тогда как уровень проваливает T1 на любой сетке. Но T3 не проходит ни одно окно: CV(κ) по окнам 0.74 и выше при пороге 0.50 — скорость возврата не постоянна, она меняется вместе с режимом волатильности.

Поэтому вердикт «не OU» устоял, но **основание у него другое**, чем записано в 69: дело не в нестационарности того, что торгуется, а в нестабильности κ. Практический смысл этого — размер позиции и горизонт выхода, посчитанные по одному κ, недействительны вне того режима, где κ измерен; направление не оживает.

**Синтетический контроль (§7.1, `BasisVerifyTest`).** На заведомом OU с полупериодом 1.7 мин и шагом 1 мин: на полной сетке κ восстанавливается (0.4085 против истинной 0.4077), на шаге 5 мин смещение +1.4%, на шаге 28 мин **−47%** — полупериод завышается с 1.7 до 3.2 мин. Вердикт T1 прореживание переживает, а T3 на шаге 28 мин **переворачивается** (заведомо стабильная κ признаётся нестабильной). Отсюда читается и таблица выше: строка `THIN_AS_RUN` с CV 2.32 — артефакт сетки, а строка `FULL` с CV 0.74 — нет. В прогоне 69 T3 считался на полном ряде, прореживались только T1 и T4, поэтому подмены вердикта там не произошло.

## Модуль 2: природа стресс-эпизодов (Q5)

**Правило отбора 26 дней, дословно:** самые волатильные дни с 2024-01-01 — объединение топ-20 по модулю дневной доходности (|close/open − 1|) и топ-20 по внутридневному размаху ((high − low)/open), по дневным свечам BTCUSDT из `candles`. Выборка отобрана **по волатильности**, поэтому частота эпизодов на ней (5 на 26 дней против 3 на 365) **не является оценкой частоты** и ниже как таковая не используется.

**И3. Отдельная калибровка.** Непрерывный ряд: κ = 0.4003/мин, полупериод 1.7 мин, θ = -0.00046, σ = 0.00009. Стресс-выборка: κ = 0.0309/мин, полупериод 22.4 мин, θ = 0.00009, σ = 0.00017. Отношение κ — **×12.94**.

**И1 и И2 по каждому эпизоду.** `R² затухания` — качество подгонки экспоненциального возврата к предэпизодному уровню; `R² ступеньки` — качество подгонки «плато и скачок» (две константы). Часы по модулю 8 — положение границы относительно расчёта funding (0 = момент расчёта).

| # | Длительность, мин | Глубина | R² затухания | R² ступеньки | Форма | старт, ч mod 8 | конец, ч mod 8 |
|---|---|---|---|---|---|---|---|
| 1 | 1 | 0.0111 | NaN | NaN | — | 4.17 | 4.18 |
| 2 | 8801 | 0.0077 | -7.501 | 0.384 | **ступенька** | 1.25 | 3.93 |
| 3 | 55840 | 0.0050 | -23.022 | 0.418 | **ступенька** | 3.97 | 6.63 |
| 4 | 312 | 0.0042 | -3.914 | 0.624 | **ступенька** | 1.18 | 6.38 |
| 5 | 1193 | 0.0071 | -0.409 | 0.677 | **ступенька** | 2.57 | 6.45 |

**Профили эпизодов** — 12 равноотстоящих точек от начала к концу, значения базиса в долях. Плато читается как ряд близких чисел, затухание — как монотонное приближение к θ.

| # | точки профиля (от начала к концу) |
|---|---|
| 1 | -0.0110 · 0.0004 |
| 2 | 0.0044 · 0.0012 · 0.0011 · 0.0010 · 0.0013 · 0.0010 · 0.0010 · 0.0013 · 0.0015 · 0.0016 · 0.0016 · -0.0003 |
| 3 | 0.0051 · 0.0007 · 0.0009 · 0.0008 · 0.0008 · 0.0007 · 0.0008 · 0.0005 · 0.0006 · 0.0004 · 0.0004 · 0.0001 |
| 4 | -0.0041 · -0.0005 · -0.0004 · -0.0004 · -0.0004 · -0.0005 · -0.0004 · -0.0005 · -0.0006 · -0.0005 · -0.0002 · 0.0004 |
| 5 | 0.0066 · 0.0015 · 0.0013 · 0.0006 · 0.0009 · 0.0012 · 0.0010 · 0.0004 · 0.0008 · 0.0006 · 0.0008 · -0.0012 |

### Вердикт модуля 2

Классифицируемых эпизодов (длиннее четырёх точек) — 4 из 5. Ступенькой лучше описываются **4 из 4**; концов эпизодов в пределах часа от расчёта funding — **0 из 4**.

Сравнение относительное, поэтому отдельно об абсолютном качестве: лучшая подгонка ступеньки — R² 0.68, лучшая подгонка затухания — R² -0.41. У затухания R² отрицателен на большинстве эпизодов, то есть экспоненциальный возврат к θ описывает их **хуже, чем просто константа**. Это сильное свидетельство против «отклонения с возвратом», но не свидетельство в пользу чистой ступеньки: её R² тоже далёк от единицы.

**Что видно в профилях.** Пик спадает быстро: к первой двенадцатой эпизода уходит в среднем 83% глубины — это совместимо с полупериодом в минуты. Но дальше величина не возвращается к θ, а **висит на смещённом уровне** — медиана плато 0.00079 при θ = 0.00009, — и длительность эпизода набирается именно этим висением, а не спадом. Отсюда и 8801 и 55840 минут: эпизод по определению длится до пересечения θ, а пересечение откладывается на дни.

Практический вывод: длительность эпизода здесь **не характеристика процесса, а свойство определения** — ни «сдвиг уровня», ни «отклонение с возвратом» в чистом виде не подходят. Верное описание — быстрый спад к смещённому уровню плюс медленный дрейф этого уровня; торговать в такой форме можно только сам спад, то есть первые минуты, где издержки и сравнимы с амплитудой.

Картина смешанная: форма и привязка к funding указывают в разные стороны. Вывод не форсируется — при пяти эпизодах это описание, а не статистика.

## Модуль 3: сверка констант S5 со спецификацией (Q7)

| Величина | В коде | Спецификация | Совпадает | Где используется |
|---|---|---|---|---|
| `entry_lead` | 5 | 5 (док. 02 v2 §S5 «вход за 5 дней до события») | да | S5Config.java:13 → S5Orchestrator:141 (вход на unlockDay−entryLead) |
| `min_pct_supply` | 0.03 | 0.03 (док. 02 v2 §S5 «разлок ≥ 3% циркулирующего supply») | да | UnlockFeed.java:19 → UnlockFeed:115 (отсев события) |
| `expensive_funding_threshold` | -0.015 | -0.015 (док. 02 v2 §S5 «отмена при ожидаемом funding > 1.5%») | да | S5Config.java:13 → S5Orchestrator:88 (пропуск дорогого шорта) |
| `exit_on_unlock_day` | 1 | 1 (док. 02 v2 §S5 «выход в день разлока до разблокировки») | да | S5Orchestrator:209 (плановый выход при unlockDay ≤ today) |
| `position_fraction` | 0.045 | 0.02 (док. 02 v2 §S5 «≤ 2% общего капитала на событие») | **НЕТ** | S5Config.java:13 → S5Orchestrator:112 (размер позиции) |
| `max_concurrent` | 18 | 3 (док. 02 v2 §S5 «≤ 3 одновременных события») | **НЕТ** | S5Orchestrator:256 (wouldBreachExposure: maxExposure/positionFraction) |
| `stop_frac` | 0.3 | 0.08 (док. 02 v2 §S5 «стоп +8% против позиции → выход») | **НЕТ** | S5Config.java:13 → StopEngine:55,59 (уровень стопа и биржевой stop-buy) |
| `recipient_filter` | 0 | 1 (док. 02 v2 §S5 «получатели — инвесторы/команда») | **НЕТ** | UnlockFeed.classify считает категорию; S5Orchestrator её только показывает |
| `approval_lead_days` | 2 | - (в док. 02 v2 отсутствует) | — | S5Config.java:13 → S5Orchestrator:83 (окно всплытия кандидата) |

**Расхождений: 4.**

- **`position_fraction`** — в коде 0.045, в спецификации 0.02. Код следует протоколу док. 54 §«Позиция ≤4.5%, суммарная экспозиция ≤81%», выведенному из кластеризации 18 одновременных событий (док. 52 §3.3). Спецификация 02 v2 от 06.08 просто не обновлена
- **`max_concurrent`** — в коде 18, в спецификации 3. В коде лимита на число позиций нет: ограничивается суммарная экспозиция (S5Orchestrator:256), и при 4.5% на позицию потолок 81% допускает 18 одновременных. То же расхождение, что и position_fraction, только в другой форме
- **`stop_frac`** — в коде 0.3, в спецификации 0.08. ОТКРЫТЫЙ ВОПРОС, а не устаревшая строка: 30% выбран по сетке в reports/s5_checks.md §A (13.08.2026) и зафиксирован протоколом док. 54, но измерение ТЗ 66 показало, что при 8% стоп срабатывает в 46% событий и премия падает с +2.17% до +0.39%. Решать отдельно: править код или править спецификацию
- **`recipient_filter`** — в коде 0, в спецификации 1. Категория получателя вычисляется (UnlockFeed.classify) и показывается в уведомлениях, но НЕ фильтрует отбор: в работу идут и ecosystem, и staking. Расширение вселенной относительно спецификации

По §6.2 ТЗ это уже не случайность: **спецификация и код разошлись системно**. Три из четырёх расхождений — след того, что док. 02 v2 (06.08) не обновлялся после протокола док. 54 (14.08); четвёртое (фильтр получателей) не следует ни из одного документа и выглядит просто неперенесённым требованием.

**Как это больше не повиснет незамеченным.** Реестр значений спецификации вынесен в `src/main/resources/s5-spec.tsv` — одна таблица, два потребителя: этот отчёт и автотест `S5SpecComplianceTest`. Зарегистрированное расхождение фиксирует **обе** стороны: тест падает и если поменяется код, и если перепишут строку спецификации, и если код уедет на третье значение. Проверено: при снятии регистрации со стопа тест падает с сообщением «код 0.3, спецификация 0.08 (док. 02 v2 §S5)».

**Формулировка обоснования стопа (§5.3).** Не «−30% оптимален», а: премия плоская для стопов ≥30% (без стопа +1.53%, −30% +1.57%, −50% +1.64% — разброс 0.11 п.п.), поэтому уровень выбирается **по худшей сделке**, а не по средней: −34% против −189% без стопа при нулевой цене в средней. Оговорка: премия на −30% измерена на тех же данных, где −30% выбирался; на плоском участке смещение мало, но оно есть. Отдельно и против: измерение ТЗ 66 показало, что при стопе 8% из спецификации стоп срабатывает в 156 событиях из 339 и премия падает до +0.39% — то есть выбор между 8% и 30% меняет не размер, а сам факт наличия премии.

## Оговорки

- Ряд `BASIS` — минутные свечи, обе ноги с одной площадки и с общим `close_time`: рассинхронизация нулевая по построению.
- `DEVIATION` — уточнение предмета (торгуются отклонения, не уровень), а не второй шанс для вердикта: если и оно не проходит, «не OU» подтверждён.
- Частота эпизодов на стресс-выборке не используется как оценка частоты.
- Модуль 3 ничего не исправляет: список расхождений — вход для решений, каждое закрывается отдельно.
